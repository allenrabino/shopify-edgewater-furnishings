
<div class="section-background color-{{ section.settings.color_scheme }}"></div>
<div
  class="section section--{{ section.settings.section_width }} color-{{ section.settings.color_scheme }} customer-reviews spacing-style"
  style="{% render 'spacing-style', settings: section.settings %}"
>
  <div class="customer-reviews__inner page-width">
    <header class="customer-reviews__header">
      {%- if section.settings.heading != blank -%}
        <h2 class="customer-reviews__heading">{{ section.settings.heading }}</h2>
      {%- endif -%}

      {%- if section.settings.show_summary -%}
        <div class="customer-reviews__summary">
          <div
            class="customer-reviews__summary-stars"
            aria-label="{{ section.settings.summary_rating }} out of 5 stars"
          >
            {%- assign summary_rating = section.settings.summary_rating | round -%}
            {%- for i in (1..5) -%}
              <span class="customer-reviews__summary-star{% if i <= summary_rating %} customer-reviews__summary-star--filled{% endif %}" aria-hidden="true">★</span>
            {%- endfor -%}
          </div>
        </div>
      {%- endif -%}

      {%- if section.settings.description != blank -%}
        <div class="customer-reviews__description rte">{{ section.settings.description }}</div>
      {%- endif -%}
    </header>

    {%- liquid
      assign carousel_columns = section.settings.columns | default: 3
      assign mobile_columns = section.settings.mobile_columns | default: 1
      assign review_slide_count = 54
    -%}

    <div class="customer-reviews__carousel">
      {% capture review_slides %}
        {% render 'edgewater-reviews-slides' %}
      {% endcapture %}

      {% capture carousel_style %}
        --carousel-columns: {{ carousel_columns }};
        --carousel-mobile-columns: 1;
        --carousel-tablet-columns: 2;
        --carousel-gap: 1.25rem;
      {% endcapture %}

      {% render 'slideshow',
        ref: 'customerReviewsCarousel',
        class: 'customer-reviews-carousel',
        style: carousel_style,
        slides: review_slides,
        show_arrows: true,
        icon_style: 'arrow',
        icon_shape: 'circle',
        auto_hide_controls: false,
        infinite: true,
        initial_slide: 0,
        slide_count: review_slide_count,
        slide_position: 'stretch',
        arrows_position: 'center',
        attributes: 'data-autoplay="true" data-autoplay-interval="5000"'
      %}
    </div>
  </div>
</div>

<script src="{{ 'customer-reviews-carousel.js' | asset_url }}" defer></script>

{% stylesheet %}
  .customer-reviews__inner {
    display: flex;
    flex-direction: column;
    gap: var(--gap-xl);
    padding-inline: max(1rem, var(--page-margin, 16px));
  }

  @media screen and (min-width: 750px) {
    .customer-reviews__inner {
      gap: var(--gap-2xl);
      padding-inline: max(1.5rem, var(--page-margin, 40px));
    }
  }

  .customer-reviews__header {
    text-align: center;
    max-width: 40rem;
    margin-inline: auto;
    padding-inline: 0.25rem;
  }

  .customer-reviews__heading {
    margin: 0 0 var(--gap-sm);
    font-size: clamp(1.375rem, 5vw, 2.25rem);
    font-weight: 600;
    line-height: 1.2;
  }

  @media screen and (min-width: 750px) {
    .customer-reviews__heading {
      margin-bottom: var(--gap-md);
    }
  }

  .customer-reviews__summary {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--gap-xs);
    margin-bottom: var(--gap-sm);
  }

  @media screen and (min-width: 750px) {
    .customer-reviews__summary {
      margin-bottom: var(--gap-md);
    }
  }

  .customer-reviews__summary-stars {
    display: flex;
    gap: 0.2rem;
    font-size: clamp(1rem, 3vw, 1.25rem);
    line-height: 1;
  }

  .customer-reviews__summary-star {
    color: rgb(var(--announcement-bar-color-rgb) / 0.25);
  }

  .customer-reviews__summary-star--filled {
    color: var(--announcement-bar-color);
  }

  .customer-reviews__description {
    font-size: clamp(0.9375rem, 2.5vw, var(--font-size--md));
    color: rgb(var(--color-foreground-rgb) / var(--opacity-subdued-text));
    line-height: 1.6;
  }

  .shopify-section:has(.customer-reviews),
  .shopify-section:has(.customer-reviews) .section-background {
    background-color: #f4efe8;
  }

  .customer-reviews {
    background-color: transparent;
    --customer-review-card-height: 15.5rem;
  }

  @media screen and (min-width: 750px) {
    .customer-reviews {
      --customer-review-card-height: 17rem;
    }
  }

  @media screen and (min-width: 1024px) {
    .customer-reviews {
      --customer-review-card-height: 18.5rem;
    }
  }

  .customer-reviews__carousel {
    position: relative;
    width: 100%;
    overflow: hidden;
  }

  .customer-reviews-carousel {
    --carousel-gap: 1rem;
  }

  @media screen and (min-width: 750px) {
    .customer-reviews-carousel {
      --carousel-gap: 1.25rem;
    }
  }

  .customer-reviews-carousel slideshow-container {
    position: relative;
    display: block;
    background-color: transparent;
    padding-inline: 3.25rem;
  }

  .customer-reviews-carousel slideshow-container > slideshow-slides {
    position: relative;
    z-index: 1;
    --slideshow-gap: var(--carousel-gap, 1.25rem);
    align-items: stretch;
    scroll-behavior: smooth;
    -webkit-overflow-scrolling: touch;
  }

  .customer-reviews-carousel slideshow-container > slideshow-arrows {
    position: absolute !important;
    inset: 0 !important;
    z-index: 2;
    display: flex !important;
    justify-content: space-between;
    align-items: center;
    width: 100%;
    height: 100%;
    margin: 0;
    padding: 0;
    pointer-events: none;
    mix-blend-mode: normal;
  }

  .customer-reviews-carousel slideshow-container > slideshow-arrows .slideshow-control {
    opacity: 1 !important;
    position: static;
    transform: none !important;
    animation: none !important;
    pointer-events: auto;
    flex-shrink: 0;
    min-height: 2.75rem;
    min-width: 2.75rem;
    padding: 0;
    border-radius: 50%;
    background-color: var(--announcement-bar-color);
    color: var(--announcement-bar-text);
  }

  .customer-reviews-carousel slideshow-container > slideshow-arrows .slideshow-control:hover {
    background-color: var(--announcement-bar-color-hover);
    color: var(--announcement-bar-text);
  }

  .customer-reviews-carousel slideshow-container > slideshow-arrows .slideshow-control[disabled] {
    opacity: 0.35;
  }

  @media screen and (max-width: 767px) {
    .customer-reviews-carousel slideshow-container {
      display: flex;
      flex-direction: column;
      gap: 1.25rem;
      padding-inline: 0;
    }

    .customer-reviews-carousel slideshow-container > slideshow-slides {
      order: 1;
      width: 100%;
    }

    .customer-reviews-carousel slideshow-container > slideshow-arrows {
      position: static !important;
      inset: auto !important;
      order: 2;
      width: auto;
      height: auto;
      justify-content: center;
      gap: 1.5rem;
      pointer-events: auto;
    }

    .customer-reviews-carousel slideshow-container > slideshow-arrows .slideshow-control {
      min-height: 2.5rem;
      min-width: 2.5rem;
    }
  }

  .customer-reviews-carousel slideshow-slides > slideshow-slide,
  .customer-reviews-carousel slideshow-slides > .customer-reviews__slide {
    flex: 0 0 auto;
    height: auto;
    align-self: stretch;
    scroll-snap-align: start;
    scroll-snap-stop: always;
    width: calc(
      (100% - (var(--carousel-gap, 1.25rem) * (var(--carousel-mobile-columns, 1) - 1))) /
        var(--carousel-mobile-columns, 1)
    );
  }

  .customer-reviews-carousel slideshow-slides > slideshow-slide .customer-review,
  .customer-reviews-carousel slideshow-slides > .customer-reviews__slide .customer-review {
    height: var(--customer-review-card-height);
    min-height: var(--customer-review-card-height);
    max-height: var(--customer-review-card-height);
  }

  @media screen and (max-width: 767px) {
    .customer-reviews-carousel slideshow-slides > slideshow-slide,
    .customer-reviews-carousel slideshow-slides > .customer-reviews__slide {
      scroll-snap-align: center;
    }
  }

  @media screen and (min-width: 768px) and (max-width: 1023px) {
    .customer-reviews-carousel slideshow-slides > slideshow-slide,
    .customer-reviews-carousel slideshow-slides > .customer-reviews__slide {
      width: calc(
        (100% - (var(--carousel-gap, 1.25rem) * (var(--carousel-tablet-columns, 2) - 1))) /
          var(--carousel-tablet-columns, 2)
      );
    }
  }

  @media screen and (min-width: 1024px) {
    .customer-reviews-carousel slideshow-slides > slideshow-slide,
    .customer-reviews-carousel slideshow-slides > .customer-reviews__slide {
      width: calc(
        (100% - (var(--carousel-gap, 1.25rem) * (var(--carousel-columns, 3) - 1))) /
          var(--carousel-columns, 3)
      );
    }
  }

  .customer-review {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: var(--gap-sm);
    box-sizing: border-box;
    height: var(--customer-review-card-height, 15.5rem);
    min-height: var(--customer-review-card-height, 15.5rem);
    max-height: var(--customer-review-card-height, 15.5rem);
    padding: 1rem;
    border: 1px solid rgb(var(--color-foreground-rgb) / 0.12);
    border-radius: 12px;
    background: #fff;
    text-align: left;
    overflow: hidden;
  }

  @media screen and (min-width: 750px) {
    .customer-review {
      padding: 1.25rem;
    }
  }

  .customer-review__avatar {
    width: 3rem;
    height: 3rem;
    border-radius: 50%;
    object-fit: cover;
    flex-shrink: 0;
  }

  @media screen and (min-width: 750px) {
    .customer-review__avatar {
      width: 3.5rem;
      height: 3.5rem;
    }
  }

  .customer-review__stars {
    display: flex;
    gap: 0.15rem;
    line-height: 1;
  }

  .customer-review__star {
    font-size: clamp(0.9375rem, 2.5vw, 1.125rem);
    color: rgb(var(--announcement-bar-color-rgb) / 0.25);
    letter-spacing: 0.05em;
  }

  .customer-review__star--filled {
    color: var(--announcement-bar-color);
  }

  .customer-review__quote {
    position: relative;
    flex: 1 1 auto;
    min-height: 0;
    width: 100%;
    margin: 0;
    padding: 0 0.25rem 0 0;
    align-self: stretch;
    overflow-y: auto;
    overscroll-behavior: contain;
    -webkit-overflow-scrolling: touch;
    font-size: clamp(0.875rem, 2.5vw, var(--font-size--sm));
    line-height: 1.5;
    color: var(--color-foreground);
    font-style: normal;
    text-align: left;
    scrollbar-width: thin;
    scrollbar-color: rgb(var(--announcement-bar-color-rgb) / 0.45) transparent;
  }

  .customer-review__quote::-webkit-scrollbar {
    width: 5px;
  }

  .customer-review__quote::-webkit-scrollbar-track {
    margin-block: 0.125rem;
    background: rgb(var(--color-foreground-rgb) / 0.06);
    border-radius: 999px;
  }

  .customer-review__quote::-webkit-scrollbar-thumb {
    background: rgb(var(--announcement-bar-color-rgb) / 0.45);
    border-radius: 999px;
  }

  .customer-review__quote::-webkit-scrollbar-thumb:hover {
    background: var(--announcement-bar-color);
  }

  .customer-review__quote > *:first-child {
    margin-block-start: 0;
  }

  .customer-review__quote > *:last-child {
    margin-block-end: 0;
  }

  .customer-review__footer {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    align-self: stretch;
    flex-shrink: 0;
    gap: 0.25rem;
    margin-top: auto;
    padding-top: 0.25rem;
  }

  .customer-review__author {
    font-size: clamp(0.875rem, 2.5vw, var(--font-size--sm));
    font-weight: 700;
    font-style: normal;
    color: var(--color-foreground);
    line-height: 1.3;
  }

  .customer-review__verified {
    font-size: clamp(0.75rem, 2vw, 0.8125rem);
    color: rgb(var(--color-foreground-rgb) / 0.55);
    line-height: 1.3;
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Customer reviews",
  "class": "ui-test-customer-reviews",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Reviews"
    },
    {
      "type": "header",
      "content": "Summary"
    },
    {
      "type": "checkbox",
      "id": "show_summary",
      "label": "Show rating summary",
      "default": true
    },
    {
      "type": "range",
      "id": "summary_rating",
      "label": "Average rating",
      "min": 0,
      "max": 5,
      "step": 0.1,
      "default": 5
    },
    {
      "type": "number",
      "id": "review_count",
      "label": "Total review count",
      "default": 54
    },
    {
      "type": "header",
      "content": "Carousel"
    },
    {
      "type": "range",
      "id": "columns",
      "label": "Slides per row (desktop)",
      "min": 1,
      "max": 4,
      "step": 1,
      "default": 3
    },
    {
      "type": "select",
      "id": "mobile_columns",
      "label": "Slides per row (mobile)",
      "options": [
        {
          "value": "1",
          "label": "1"
        },
        {
          "value": "2",
          "label": "2"
        }
      ],
      "default": "1"
    },
    {
      "type": "select",
      "id": "section_width",
      "label": "Section width",
      "options": [
        {
          "value": "page-width",
          "label": "Page"
        },
        {
          "value": "full-width",
          "label": "Full"
        }
      ],
      "default": "page-width"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "Padding"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "Top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 48
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "Bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 48
    }
  ],
  "presets": [
    {
      "name": "Customer reviews"
    }
  ]
}
{% endschema %}
